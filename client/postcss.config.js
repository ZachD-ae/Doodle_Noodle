module.exports = {
  plugins: [
    // Other plugins like autoprefixer if you have them
    require('autoprefixer'),
    require('@tailwindcss/postcss'), // This is the new Tailwind PostCSS plugin
  ],
}
